import { a as listArticles } from "./articles.functions-D2OsHuPk.js";
import { queryOptions } from "@tanstack/react-query";
//#region src/routes/discover.tsx?tsr-shared=1
var discoverQuery = (category, country) => queryOptions({
	queryKey: [
		"discover",
		category ?? "all",
		country ?? "world"
	],
	queryFn: () => listArticles({ data: {
		limit: 24,
		offset: 0,
		category,
		country
	} }),
	staleTime: 3e4
});
//#endregion
export { discoverQuery as t };
