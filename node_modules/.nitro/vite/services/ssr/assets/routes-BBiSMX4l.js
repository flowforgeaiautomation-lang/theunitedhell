import { a as listArticles } from "./articles.functions-DAhbAunG.js";
import { queryOptions } from "@tanstack/react-query";
//#region src/routes/index.tsx?tsr-shared=1
var homeQuery = (category, country) => queryOptions({
	queryKey: [
		"home",
		category ?? "all",
		country ?? "world"
	],
	queryFn: () => listArticles({ data: {
		limit: 24,
		offset: 0,
		category,
		country,
		todayOnly: true
	} }),
	staleTime: 3e4
});
//#endregion
export { homeQuery as t };
